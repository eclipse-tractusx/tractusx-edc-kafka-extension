meta {
  name: Get EDR
  type: http
  seq: 6
}

get {
  url: {{CONSUMER_MANAGEMENT_URL}}/v3/edrs/{{CONSUMER_TRANSFER_ID}}/dataaddress
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

docs {
  ```
  [only for pull transfer] Get EDR (insert id from 'initiate transfer')
  
   ```
}
