# Configuration for helm/chart-releaser-action
# Restrict releases to specific charts and keep action idempotent.
chart-dirs:
  - charts
skip-existing: true
packages-with-index: true
# Exclude charts that should not be released
skip-charts:
  - consumer-app
  - producer-app
